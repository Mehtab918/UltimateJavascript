var a = 10;
a = true;
a ="Mehtab";
// Js is not a strictly type programming language.
console.log("Welcome to learn JS : " + a);

// right side value is literal
// left side is identifiers
// = is assignment operator

const { log } = require("console");
const { stringify } = require("querystring");

console.log(45);
var a = 20;
console.log(a);
console.log(b); //undefined.
var b = 230;
//var keyword is used to declare a variable.
// var keyword was being used before ES6 features.

/* 
Note : 

Variables are container for storing data values.
JavaScript variables are containers for storing data values.
In this example, x, y, and z, are variables, declared with the var keyword. */

var ab = "Hello";
var cd = "World";
var ef = ab +" "+ cd;

console.log(ef);

// var keyword is used in Ecmascript 5.

//let keyword introduced in Es6.

let x = "Hello";
console.log(x);

x = 25; // changing the value of x.
console/log(x);   // new value of x is 25.

// Javascipt is a smart language which can change the value of variable at run time.

//variable can be declared using var, let and const.
// var is globally scoped while let and const are block scoped.
// var can be updated and re-declared within its scope.

// numbers, string, boolean, null, undefined, symbol values can be stored as a variables.

// Rules and regulations to write a variable in javascript
// - The name must begin with a letter, underscore, or dollar sign.
// 
  
// variables cannot be start with number . 